# Codex

Use the server-specific README snippet for the binary you want, then place the same `command` and `env` block in your Codex MCP configuration.

Prefer `command: "mssql-mcp"` style entries when the binary is on `PATH`. Use an absolute path only when needed.
