# VS Code

Use the same MCP JSON shape as the per-server README examples and register the binary plus environment variables in your VS Code MCP settings.

Prefer the bare binary name when it is on `PATH`, for example `command: "postgres-mcp"`.
